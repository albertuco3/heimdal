{% extends '/backend.html.twig' %}

{% block content %}
    <div class="row">
        <div class="col-md-12">
            <h2>Trazabilidad por pedido</h2>
        </div>
    </div>
    <form class="row" method="post">
        <div class="col-md-3">
            <div class="card">
                <div class="card-body">
                    <div class="mb-3">
                        <label class="form-label">Nº Pedido</label>
                        <input type="text" name="deliveryNoteId" class="form-control" value="{{ deliveryNoteId }}">
                    </div>

                    <div>
                        <button type="submit" class="btn btn-primary" style="width: 100%">Buscar</button>
                    </div>
                </div>
            </div>
        </div>
        <div class="col-md-9">
            {% if deliveryNoteId %}
                <div class="card">
                    <Box :items="{{ articles | json_encode  }}"></Box>
                </div>
            {% endif %}
        </div>
    </form>
{% endblock %}

{% block toolbar %}

{% endblock %}

